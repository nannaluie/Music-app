FROM ubuntu:latest
LABEL authors="nanna"

ENTRYPOINT ["top", "-b"]
version: '3.8'

services:
  db:
    image: postgres:15
    container_name: postgres_db
    restart: always
    environment:
      POSTGRES_USER: nannaluie
      POSTGRES_PASSWORD: zzZz2211
      POSTGRES_DB: music-app-nanni20
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: